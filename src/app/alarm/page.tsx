import AlarmItem from '@/app/alarm/(alarm)/AlarmItem';

const AlarmPage = () => {
  return (
    <div className="h-full flex flex-col sm:mb-0">
      <AlarmItem />
    </div>
  );
};

export default AlarmPage;
