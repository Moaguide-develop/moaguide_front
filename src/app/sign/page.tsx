'use client';

import SignLayout from '@/app/sign/(sign)/SignLayout';
import React from 'react';

const SignPage = () => {
  return (
    <div>
      <SignLayout />
    </div>
  );
};

export default SignPage;
